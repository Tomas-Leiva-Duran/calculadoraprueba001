import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.calculadoraprueba001',
  appName: 'calculadoraprueba001',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
